" vim-surround ships with y-prefix mappings by default. This introduces cursor
" lag in workman mode. Disable the bindings here, and set the manually in the
" keyboard section.
let g:surround_no_mappings=1
