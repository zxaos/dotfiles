Host *
    IdentityAgent "~/Library/Group Containers/2BUA8C4S2C.com.1password/t/agent.sock"
    ControlMaster auto
    ControlPath ~/.ssh/sock/%r@%h:%p
    # ControlPath ~/.ssh/sock/%C # For path too long issues, use the hash instead
    ControlPersist yes
